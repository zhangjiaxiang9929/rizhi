# 2026-03-20

## BTC 合约交易机器人 代码审查与修复

### 项目信息
- 项目根目录: `D:\gate bot\`
- 源代码: `D:\gate bot\src\`（engine.js, main.js, ai-advisor.js, index.html）
- 数据目录: `D:\gate bot\data\`
- 运行模式: 模拟模式 (`dryRun: true`)
- 合约: `BTC_USDT`
- 交易所: Gate.io (`api.gateio.ws`)
- AI模型: `deepseek-v3.2阿里云` via `api2.openclawcn.net`

### 修复的Bug（本次会话）
1. **错误文件路径（trade_history）**: `src/data/` → `data/`，用 `path.join(__dirname, "..", "data")` 修复
2. **空指针错误**: `Cannot read properties of null (reading 'time')` — 在访问 `this.pos.time` 之前加了 `if (!this.pos) return;`
3. **报告文件路径**: `generateDailyReport()` 中报告文件路径同样改用 `__dirname, "..", "data"`
4. **main.js HIST_FILE**: 改为 `path.join(__dirname, "..", "data", "trade_history_BTC_USDT.json")`
5. **sim:clear 路径**: main.js 中清除模拟数据的路径也修正为 `__dirname, "..", "data"`

### 全量代码审查结论（04:35 UTC）
- ai-advisor.js ✅ 无问题
- engine.js ✅ 无问题（所有路径和逻辑已修正）
- main.js ✅ 无问题
- index.html ⚠️ 发现一个小问题：`clearSimData()` 函数被按钮调用但未定义
  - 修复方案：添加 `function clearSimData() { ipcRenderer.send("sim:clear"); ... }`

### 后续新增修复（05:00-05:40 UTC）

6. **关闭窗口确认弹窗**: `win.on("close")` 加 `e.preventDefault()` + `dialog.showMessageBoxSync` 确认框，运行中提示"持仓将不再被监控"
7. **CNY切换后数学预期不更新**: `updateMath()` 所有金额改用 `formatMoney()` 函数，切换货币后自动同步
8. **CSV导出路径**: 改为 `dialog.showSaveDialog` 弹窗选择，默认文件名 `btc_trades_YYYY-MM-DD.csv`
9. **clearSimData 函数**: index.html 中补充定义该函数 ✅

### 订单簿大单信号讨论（05:39 UTC）
- 大单信号走 `startOrderBookWatcher()` 流程，**不经过 AI 确认**，直接开仓
- 5分钟策略信号走 `run()` 流程，**经过 aiAdvise() 确认**
- 用户询问是否需要加AI确认 → 建议加，理由：
  - 低波动(0.15%)时大单信号容易是假信号
  - 当前那笔单持仓15分钟净利一直为负（手续费3.99U > 毛盈利）
  - 建议方案：低波动(<0.3%)时才过AI审核，高波动(>0.5%)直接开，兼顾速度和准确性
- **待实现**：为大单信号加可选AI确认逻辑（`cfg.aiCheckOrderBook` 开关 + 低波动才触发）

### 用户偏好
- 全程使用中文回复

---

## ETH 合约交易机器人转换（06:40–09:00 UTC）

### 任务
把 BTC bot（`D:\gate bot\`）转换为 ETH bot，放到 `D:\gate bot eth\`

### 用户发来的最新版本文件（4个）
- `src/ai-advisor.js` — AI分析模块
- `src/engine.js` — 核心引擎
- `src/index.html` — 前端界面
- `src/main.js` — Electron主进程

### 与旧工作区版本的差异
用户发来的版本比 `/home/node/.openclaw/workspace/eth-bot/` 更新，包含：
- AI主导模式 (`aiDriven`)
- 多周期K线分析（1h/4h/1d）
- 大单信号AI低波动确认
- 历史胜率统计 (`getWinStats`)
- 每日报告 (`generateDailyReport`)
- 日志写入文件 (`bot_YYYY-MM-DD.log`)
- AI决策日志缓冲 (`aiDecisionLog`)
- 多空比/持仓量指标

### ETH 关键修改点（已向用户说明）
1. `BTC_USDT` → `ETH_USDT` 全局替换
2. `mult: 0.0001` → `mult: 0.001`（ETH每张=0.001 ETH）
3. `trade_history_BTC_USDT.json` → `trade_history_ETH_USDT.json`
4. `btc_trades_*.csv` → `eth_trades_*.csv`
5. `BTC Bot` 文字 → `ETH Bot`
6. `currentSymbol = "BTC_USDT"` → `"ETH_USDT"`（index.html）
7. `<title>BTC` → `<title>ETH`

### 分段发送进度（engine.js）
- 第1段：constructor / fetchExchangeRate / start / updateConfig / stop / tick ✅ 已发
- 第2段：startPriceTicker / startOrderBookWatcher / _setConn / scoreSignal ✅ 已发
- 第3段：run() 完整方法 ✅ 已发（09:00 UTC 截止时还在发送中）
- 第4段：startDailyReportTimer / saveSimState / loadSimState / clearSimState / loadTradeHistory / saveTradeHistory / generateDailyReport / getWinStats — 待发
- 第5段：指标计算 ema/rsiCalc/atrCalc/bbCalc/adxCalc/buildIndicators/isBullC等/getSignal — 待发
- 第6段：API方法 request/fetchKlines/getOrderBook/getFundingRate/getBalance/getLongShortRatio/getOpenInterest/getPrice/getPosition/getContractInfo/testAPI/setLeverage/placeOrder/closePosition/log/partialClose/setTP/setSL/runAiDriven + module.exports — 待发

### 待完成
- [ ] 发送 engine.js 第4、5、6段
- [ ] 发送 main.js 改好的完整版
- [ ] 发送 ai-advisor.js 改好的完整版（改2处BTC→ETH文字）
- [ ] 确认 index.html 修改清单（用户是否需要完整版？）

---

## BTC Bot 继续修复（06:17–11:46 UTC）

### 用户发来完整 index.html（06:17 UTC）
用户发来了完整的 `index.html` 源码，已确认：
- tab-history 压缩布局已就位（5格统计、60px图表、flex:1表格）
- exportCSV / clearHistory 按钮已存在
- clearSimData 函数已定义
- drawChart() 存在括号结构bug（posLines if块吞掉了价格标签和时间轴代码）

### 修复：drawChart() 括号结构bug
**问题**：`if (position-card)` 块的 `}` 位置错误，价格标签和时间轴代码被包在了if里，导致无持仓时K线时间轴不显示。
**修复**：在 `posLines forEach` 结束后、`const curP =` 之前加一个 `}` 关闭if块。

### 修复：账户余额不更新
**问题**：平仓后账户余额始终显示初始值 10000.00U，不随盈亏变化。
**根本原因**：engine.js 中所有平仓逻辑（止损/止盈/AI平仓/手动部分平仓）在更新 `simBalance` 后没有 `this.emit("balance", {...})` 通知UI。
**修复位置**：4处 `simBalance +=` 之后（第225、750、801、1647行附近），在 `simMargin = 0` 或 `simMargin -= releasedMargin` 之后加：
```javascript
this.emit("balance", {
  balance: this.simAvail,
  start: this.startBal,
  total: this.simBalance,
  margin: this.simMargin,
  pnl: this.simTotalPnl
});
```
同时 index.html 中 `s-balance` 的显示改为 `formatMoney(data.total || data.balance)` ✅
**状态**：4处全部加完，等待验证。

### 修复：大单AI确认两个错误
1. `Cannot read properties of null (reading 'toFixed')` — `cur5m_ai.atr` 为null，传给aiAdvise时加默认值：`atr: cur5m_ai.atr || price * 0.008`
2. `Cannot access 'margin' before initialization` — AI拒绝时退款逻辑在margin声明前执行，修复为直接return不退款（因为AI拒绝时还未扣保证金）

### 修复：AI太保守导致大单信号全被拒
所有大单信号都被AI以"置信度55%"拒绝，选择了方案B：关闭低波动AI确认：
```javascript
if (false && volatility < 0.3) { // 暂时关闭AI确认
```

### 修复：emit balance位置顺序
emit必须在以下4行全部执行完之后：
1. `simAvail += simMargin`
2. `simBalance += pnl`
3. `simTotalPnl += pnl`
4. `simMargin = 0`

### 当前运行状态（11:46 UTC）
- 模拟模式运行中，今日已交易4笔
- 今日盈亏: +22.50U (+0.22%)
- 胜率66.7%（3笔中2胜）
- 账户余额显示bug尚未通过实际交易验证（4处emit刚加完）
- BTC价格约 71,200 USDT

---

## AI完全自主模式实现（13:32 UTC）

### 用户发来完整的4个源文件
收到完整版：ai-advisor.js、engine.js、index.html、main.js（均为最新版本）。

### 已完成的功能（确认状态）
- AI模型已切换为 `qwen3-max-2026-01-23阿里云特价`（ai-advisor.js + main.js）
- AI主导模式开关 (`aiDriven`) 已在 index.html 中显示（含提示框）
- `runAiDriven()` 方法已在 engine.js 中存在（但止盈止损未用AI建议值）
- `aiDecisionLog` 收集AI决策，注入对话AI上下文 ✅
- 对话AI标注已改为"千问 3 MXA"（index.html clearAIChat仍显示"DeepSeek"，待改）

### AI完全自主模式改进方案（本次建议）

**问题1**：`runAiDriven()` 里 AI 返回了 `suggestSL/suggestTP` 但**没有被使用**，仍用ATR计算
**问题2**：AI主导模式下仓位没有动态跟随AI置信度调整（ratio字段）

**第1处改动 — ai-advisor.js `buildPrompt()` 约第180行**：
将 `aiDrivenMode` 分支的JSON要求改为：
```
{"action":"open或skip","confidence":0到100整数,"direction":"LONG或SHORT","reason":"20字内","suggestSL":止损价格数字,"suggestTP":止盈价格数字,"ratio":仓位比例0.2到1.0}
```
止盈空间必须是止损空间的1.5倍以上。ratio高置信度用1.0，中等0.5，低0.2。

**第2处改动 — engine.js `runAiDriven()` 约第580行**：
替换止盈止损计算逻辑：
1. 优先使用 `aiResult.suggestSL/suggestTP`（有效范围：相对price的5%内）
2. 无效时回退到订单簿支撑/压力位，再回退ATR
3. 新增 `aiRatio`：从 `aiResult.ratio` 读取，无效时根据置信度换算（>=85→1.0，>=70→0.7，else→0.5）
4. `contracts` 计算乘以 `aiRatio`

### parseAIResponse 增强（确认已有）
```javascript
direction: result.direction || null,
suggestSL: result.suggestSL ? parseFloat(result.suggestSL) : null,
suggestTP: result.suggestTP ? parseFloat(result.suggestTP) : null,
```
`ratio` 字段需补充解析：`ratio: result.ratio ? parseFloat(result.ratio) : null`

### 待验证问题
- 账户余额UI不更新（9992.36 vs 10000）：4处emit已加，等实际平仓后截图验证
- index.html clearAIChat 函数里仍有"DeepSeek"文字，需改为"千问3" (cosmetic)

### 当前配置
- API Key: `sk-c8ltKB9BbxxMteuEr0xja9N6O3uD68ykmTggXEcEPMsulvM2`
- API Base: `https://api2.openclawcn.net`
- AI模型精确名称: `qwen3-max-2026-01-23阿里云特价`（含中文后缀）
- 项目路径: `D:\gate bot\src\`，数据: `D:\gate bot\data\`

---

## 下午会话追加（14:00 UTC）

### 代码全量检查结果（14:01 UTC）
对用户发来的完整 ai-advisor.js + engine.js 做了全面审查：

**ai-advisor.js 状态：**
- `parseAIResponse` 包含 `ratio` 字段 ✅
- `aiAdvise` return 包含 `ratio: result.ratio` ✅
- `buildPrompt` aiDrivenMode 分支用单引号，无语法错误 ✅
- 模型名 `qwen3-max-2026-01-23阿里云特价` ✅

**engine.js 状态：**
- `runAiDriven()` 中 `aiRatio` 在 `contracts` 之前声明 ✅
- `contracts` 计算乘了 `aiRatio` ✅
- `aiSLValid2 / aiTPValid2` 优先用AI止损止盈 ✅
- 回退到订单簿/ATR逻辑正常 ✅
- `run()` 中 `aiSLValid / aiTPValid` 同样优先用AI止损止盈 ✅
- 底部没有重复的 `aiRatio` 声明 ✅

**发现遗留问题（不影响运行）：**
- `start()` 方法中日志仍写 `deepseek-v3.2`（cosmetic），建议改为 `Qwen3-Max`

### AI完全自主模式验证方法
开启AI主导模式后，看日志是否出现：
- `🤖 AI主导模式 | 主动获取数据分析中...` → 主导模式在运行
- `🤖 AI主导止损:xxxxx 止盈:xxxxx` → AI止损止盈被采用
- `💰 AI仓位比例:xx% 置信度xx%` → 动态仓位在工作

### Qwen3-Max 多模态能力确认（14:11 UTC）
- 用户指出 Qwen3-Max 支持图片 → 确认正确
- 之前回答有误（说不支持图片），已更正
- Qwen3-Max：文字 ✅、图片 ✅、文件（部分接口）✅、视频 ❌

### 对话AI图片上传功能实现（14:11 UTC）
用户要求在对话框加图片上传功能，提供了4处改动：

**1. index.html — 输入框前加📎按钮和预览区**：
- `<input type="file" id="ai-image-input" accept="image/*">` （隐藏）
- 📎按钮触发文件选择
- 预览区显示缩略图，带 × 删除按钮

**2. index.html — 新增JS函数**：
- `aiImageBase64`：全局变量存图片base64
- `onAiImageSelected(input)`：FileReader读图片转base64
- `clearAiImage()`：清除图片变量和预览

**3. index.html — `sendAIChat()` 修改**：
- `ipcRenderer.invoke("ai:chat", {..., imageBase64: aiImageBase64})`
- 发送后自动调用 `clearAiImage()` 清除图片

**4. main.js — `ai:chat` 处理器修改**：
- 参数加 `imageBase64`
- 最后一条用户消息如有图片，改为多模态格式：
  ```json
  [{"type":"image_url","image_url":{"url":"data:image/...;base64,..."}},{"type":"text","text":"..."}]
  ```

---

## 下午会话（第二轮）代码全量审查（15:55 UTC）

### 用户发来完整4个文件，请求全面审查
收到最新版：ai-advisor.js, engine.js, index.html, main.js（附完整源码）

### 审查结论

**ai-advisor.js ✅ 完全正确**
- 模型名 `qwen3-max-2026-01-23阿里云特价` ✅
- parseAIResponse 含 ratio/suggestSL/suggestTP ✅
- aiAdvise return 含 ratio ✅
- buildPrompt aiDrivenMode 三元表达式语法正确（单引号，含 else 分支） ✅
- buildCheckPrompt 重要原则1-9条 ✅
- 综合判断要点1-12条 ✅

**engine.js ⚠️ 发现1个bug**
- runAiDriven() aiRatio 在 contracts 前声明 ✅
- 冷却时间 coolSlots/coolSlots2 = 3/6/12 ✅
- 平仓后4处 emit("balance") ✅
- partialClose() notional 有兜底 ✅
- if (!this.pos) return 位置正确 ✅
- **BUG: `loadTradeHistory()` 有定义但 `start()` 中从未调用** ❌
  → 每次重启后 tradeHistory 清空，历史胜率丢失，AI无法参考历史数据
  → 修复：在 start() 里 `this.log("info", "🤖 AI辅助...")` 后加 `this.loadTradeHistory();`

**main.js ✅ 完全正确**
- bot:updateConfig 保存到 CFG_FILE ✅
- config:load 读取 CFG_FILE ✅
- 日志写入文件 ✅
- 窗口关闭确认弹窗 ✅
- CSV导出弹窗 ✅
- ai:chat 支持图片+文件 ✅
- aiDecisionLog 注入对话上下文 ✅

**index.html ⚠️ 发现3处问题**
1. `clearAIChat()` 函数仍显示"DeepSeek随时待命"（旧文字） → 改为"千问3随时待命"
2. 启动日志仍写"v14 就绪" → 改为"v15 就绪 | 策略: AI主导+5m信号+趋势过滤 | 止盈2% 止损1% | 止损冷却15min"
3. ai-image-preview 缺少 `align-items:center` style

### 已确认正常工作的功能
- AI主导模式 (aiDriven) 完整联动
- 文件上传(📄)/图片上传(📎)对话AI
- 配置持久化保存/加载
- 订单簿大单信号（AI确认已暂时关闭 if(false)）
- 每日报告定时器（UTC 15:55 = 北京23:55）
- 模拟账户余额4处emit
- 历史胜率统计+AI参考
- 多周期K线(5m/1h/4h/1d)传入AI

### 策略版本状态
- 当前策略: BTC-Hunter v15-中频稳健-AI主导
- 参数: 止盈2%, 止损1%, 杠杆5x, 仓位20%, AI置信度≥62开仓
- 冷却: 亏损≤2%→15min, 2-3%→30min, >3%→60min
- 目标: 日盈利1%, 每日6-8次开仓


---

## 第三轮全面审查（16:01 UTC）

### 用户再次发来完整4个源文件，要求仔细检查所有功能

### 发现的Bug清单（按优先级）

#### 🔴 P0 严重（导致余额反复回到10000）

**Bug 1: `run()` 方法中 `balance` 读取 `cfg.capital` 而非 `simAvail`**
- 位置: engine.js `run()` 方法
- 原因: `const balance = this.cfg.dryRun ? this.cfg.capital : ...`
- `cfg.capital` 是初始配置值（10000），不是当前余额
- 修复: 改为 `this.cfg.dryRun ? this.simAvail : ...`
- 影响: AI主导模式和普通模式开仓都用错误的余额计算合约数量

**Bug 2: `loadSimState()` 无持仓时不恢复余额**
- 位置: engine.js `loadSimState()`
- 原因: `if (state.pos) { this.simBalance = ... }` 只在有持仓时恢复余额
- 没有持仓时不恢复 simBalance/simAvail，每次重启余额清零
- 修复: 改为无论是否有持仓，只要有 `state.simBalance` 就恢复

**Bug 3: index.html 显示 `data.balance`（可用）而非 `data.total`（总资产）**
- 位置: index.html `bot:event → balance` 处理
- 原因: `balanceEl.textContent = formatMoney(data.balance)` 
- 持仓中 `data.balance = simAvail`（已扣保证金），看起来在亏
- 修复: `const displayBal = data.total != null ? data.total : data.balance;`

#### 🟡 P1 中等

**Bug 4: AI主导模式绕过冷却期**
- 位置: engine.js `run()` → AI主导分支
- 原因: `if (this.cfg.aiDriven && !this.pos && curSlot !== this.lastSlot)` 直接进入，没检查 `cooldownUntilSlot`
- 修复: 在 `runAiDriven()` 内部开头加冷却期检查

**Bug 5: `partialClose()` 减仓未保存历史**
- 位置: engine.js `partialClose()`
- 原因: emit了trade事件但没有 push 到 tradeHistory 和 saveTradeHistory()
- 修复: 在 emit trade 后加 push + saveTradeHistory()

#### 🟢 P2 小问题

**Bug 6: 冷却期显示硬编码"30min"**
- 位置: index.html 日志处理
- 修复: 从日志消息中用正则提取实际分钟数

**Bug 7: `stats-bar` 缺少 s-total/s-margin 元素**
- index.html balance事件处理器尝试找这两个元素，但DOM里没有
- 建议扩展stats-bar或在余额卡片内显示总资产/保证金

### 关键结论
余额反复回到10000 = Bug1 + Bug2 + Bug3 三个原因叠加：
- Bug1: 开仓时用 cfg.capital（10000）而非 simAvail 计算仓位
- Bug2: 重启后无持仓时 simBalance/simAvail 不恢复
- Bug3: UI显示可用余额（扣了保证金的）而非总资产

---

## 第四轮代码修复（16:40–17:13 UTC）

### 用户发来完整 engine.js，本次修复

#### 新发现：startDailyReportTimer() 和 saveSimState() 方法丢失
- 原始代码中这两个方法夹在 `run()` 结尾和 `loadSimState()` 之间
- 上次修复 loadSimState() 时不小心把这两个方法也删掉了
- 症状：代码启动时报错 `this.startDailyReportTimer is not a function`

**修复方案**：在 `run()` 最后3个 `}` 之后、`loadSimState()` 之前补充：
```javascript
startDailyReportTimer() {
  this.reportTimer = setInterval(() => {
    if (!this.running) return;
    const now = new Date(Date.now() + 8 * 3600000);
    const h = now.getUTCHours();
    const m = now.getUTCMinutes();
    if (h === 15 && m === 55 && !this._reportSentToday) {
      this._reportSentToday = true;
      this.generateDailyReport();
    }
    if (h === 16 && m === 0) {
      this._reportSentToday = false;
    }
  }, 60000);
}

saveSimState() {
  if (!this.cfg.dryRun || !this.simFile) return;
  const state = {
    pos: this.pos,
    simBalance: this.simBalance,
    simAvail: this.simAvail,
    simMargin: this.simMargin,
    simTotalPnl: this.simTotalPnl,
    startBal: this.startBal,
    lastSaveTime: Date.now()
  };
  try {
    const fs = require("fs");
    fs.writeFileSync(this.simFile, JSON.stringify(state, null, 2), "utf8");
  } catch (e) {
    this.log("error", "保存模拟状态失败: " + e.message);
  }
}
```

#### 重复方法错误（16:48 UTC）
用户第一次补充时加入了两个 `startDailyReportTimer()`，报错：
`SyntaxError: Identifier 'startDailyReportTimer' has already been declared` (ES6 class中不允许重复方法)
- 原因：补充时在 `saveSimState()` 后面又粘了一遍 `startDailyReportTimer()`
- 修复：删除第2个（`saveSimState()` 后面那个）

#### 最终正确方法顺序
```
run()
startDailyReportTimer()   ← 只有1个
saveSimState()
loadSimState()
clearSimState()
loadTradeHistory()
...
```

#### 持仓时余额显示逻辑bug（16:57 UTC 发现）
在 `run()` 持仓管理块中，嵌套了错误的条件：
```javascript
if (this.cfg.dryRun) {      // 外层：dryRun=true
  ...
  if (!this.cfg.dryRun) {  // 内层：永远false！
    emit("balance") ...    // 永远不执行
  } else {
    emit("position")...    // 一直走这里
  }
}
```
- 结果：模拟模式持仓时余额emit永远不执行

**修复方案**（替换整个持仓状态更新块）：
```javascript
floatPnl = this.pos.notional * pPct / 100;
const feeOpen = this.pos.notional * 0.0005;
const feeClose = this.pos.notional * 0.0005;
const totalFee = feeOpen + feeClose;
const netPnl = floatPnl - totalFee;
if (this.cfg.dryRun) {
  const totalAsset = this.simAvail + this.simMargin + floatPnl;
  this.emit("balance", {
    balance: this.simAvail,
    start: this.startBal,
    total: totalAsset,
    margin: this.simMargin,
    pnl: floatPnl
  });
  this.log("info", `持仓 ${dir} | ...`);
  this.log("info", `💰 浮动盈亏:...`);
} else {
  this.log("info", `持仓 ${dir} | ...`);
}
this.emit("position", { ...this.pos, price, pPct: +pPct.toFixed(2) });
```

#### 遗留Bug待修复（17:13 UTC 截止时未完成）
- `run()` 中 `balance = this.cfg.capital` → 需改为 `this.simAvail`（P0）
- 持仓时余额显示逻辑bug（上述修复方案已给出，用户还未确认应用）

### 当前文件状态（17:13 UTC）
- `startDailyReportTimer()` 和 `saveSimState()` ✅ 已补回
- 重复方法错误 ✅ 已删除第2个
- `loadSimState()` ✅ 正确版本（无持仓时也恢复余额）
- `run()` balance bug ❌ 仍用 cfg.capital
- 持仓时余额emit bug ❌ 嵌套条件写反，emit永远不执行


---

## 第五轮代码确认（17:17–17:26 UTC）

### 用户发来最新 engine.js（17:17 UTC）

#### 两个核心Bug均已修复 ✅
1. **`run()` balance bug** — `const balance = this.cfg.dryRun ? this.simAvail : ...` ✅
2. **持仓时余额emit嵌套条件bug** — 已改为平坦 if/else 结构，emit正确执行 ✅

#### 全量代码审查结论（17:17 UTC）
- `startDailyReportTimer()` ✅ 存在，仅1个
- `saveSimState()` ✅ 存在
- `loadSimState()` ✅ 正确版本（无持仓也恢复余额）
- `loadTradeHistory()` 在 `start()` 中调用 ✅
- `partialClose()` 保存历史 ✅
- 冷却期检查在 `runAiDriven()` ✅
- `aiRatio` 在 `contracts` 前声明 ✅
- AI止损止盈优先逻辑 ✅
- 4处平仓后 `emit("balance")` ✅
- **engine.js 完全正确，无遗留bug** ✅

### 剩余待处理（index.html 3处小修改）

1. **`clearAIChat()` 文字**：`"DeepSeek随时待命"` → `"千问3随时待命"`

2. **启动日志**：`"v14 就绪"` → `"v15 就绪 | 策略: AI主导+5m信号+趋势过滤 | 止盈2% 止损1% | 止损冷却15min"`

3. **账户余额显示逻辑**（`onBalance` 回调中）：
   ```javascript
   // 原来：
   balanceEl.textContent = formatMoney(data.balance);
   balanceEl.dataset.usdtValue = data.balance;
   let diff;
   if (data.pnl != null) diff = data.pnl;
   else diff = data.balance - (data.start || data.balance);
   
   // 改为：
   const displayBal = data.total != null ? data.total : data.balance;
   balanceEl.textContent = formatMoney(displayBal);
   balanceEl.dataset.usdtValue = displayBal;
   let diff;
   if (data.pnl != null) diff = data.pnl;
   else diff = displayBal - (data.start || displayBal);
   ```

### 下一步
- 用户将在 index.html 中应用上述3处修改
- 然后启动机器人验证余额显示是否正确
- 观察日志中是否出现 `🤖 AI主导止损/止盈` 和 `💰 AI仓位比例` 等字样确认AI主导模式工作正常

---

## 第六轮全量审查（17:26 UTC）

### 用户发来完整4个源文件，最终状态确认

#### ai-advisor.js ✅ 完全正确
- 模型 `qwen3-max-2026-01-23阿里云特价` ✅
- parseAIResponse 含 ratio/suggestSL/suggestTP ✅
- aiAdvise return 含 ratio ✅
- buildPrompt aiDrivenMode 单引号三元表达式 ✅
- buildCheckPrompt 原则1-9条 ✅

#### engine.js ✅ 完全正确
- run() balance 用 simAvail ✅
- 持仓时余额emit平坦if/else结构 ✅
- startDailyReportTimer() 仅1个 ✅
- saveSimState() / loadSimState() ✅
- loadTradeHistory() 在 start() 里 ✅
- partialClose() 保存历史 ✅
- 冷却期检查在 runAiDriven() ✅
- aiRatio 在 contracts 前声明 ✅

#### main.js ✅ 完全正确
- CFG_FILE 用 app.getPath("userData") ✅
- bot:updateConfig 保存到 CFG_FILE ✅
- 日志写文件 ✅
- 窗口关闭确认 ✅
- AI对话支持图片+文件 ✅
- aiDecisionLog 注入上下文 ✅

#### index.html ✅ 完全正确（用户已应用全部3处修改）
- clearAIChat() → "千问3随时待命" ✅
- 启动日志 → "v15 就绪 | 策略: AI主导+5m信号+趋势过滤..." ✅
- onBalance 余额显示 → displayBal 模式 ✅

### 策略手册 v15 完整版（17:30 UTC 应用户要求打印）

#### 基础参数
- 合约: BTC_USDT 永续合约，模拟模式
- 资金: 10,000 USDT，杠杆5x，仓位20%（AI可调）
- 止盈: 2.0%，止损: 1.0%，ATR倍数: 1.5x动态
- 每日止盈上限3%，止损上限2%，风控熔断15%

#### 信号系统
- 5m技术信号（EMA交叉/布林带突破/EMA排列）
- 评分体系（基础50分，RSI/EMA分离度/形态加分，门槛45分）
- MTF过滤（1h保守/30m激进/ADX自适应三种模式）

#### AI主导模式核心
- 每5分钟槽自动触发，AI分析5m/1h/4h/1d多周期数据
- 输入：价格/RSI/EMA/K线/订单簿/资金费率/多空比/持仓量/历史胜率
- 置信度≥62开仓，AI自主给SL/TP/ratio
- 仓位动态：置信度≥85→100%仓，≥70→70%，<70→50%
- 动态ATR倍数：波动率>0.8%→3倍，>0.5%→2倍，≤0.5%→1.5倍

#### 持仓管理
- 500ms实时止损止盈检查
- 盈利1%自动保本（止损移至入场价）
- 分批止盈（5%平30%，10%再平30%）
- AI持仓巡检（大单仓位1min，普通3min）
- 止损冷却：≤2%→15min，2-3%→30min，>3%→60min

#### 风控体系
- 每日盈亏双向限制，风控熔断，异常波动保护
- 重要数据窗口（UTC 12/14/08点前后10分钟）暂停

#### 数学预期
- 单笔止盈净利+190U，止损净亏-110U，盈亏比1:2
- 最低保本胜率36.7%
- 保守目标：日开6-8次，胜率≥55%，日盈利1%（+100U），月盈利20-30%

### HTML文档生成（17:33 UTC）
用户要求生成完整HTML说明文档，write工具出现参数问题未能写入，转为直接在聊天中提供完整HTML代码。
