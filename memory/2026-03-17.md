# 2026-03-17 日志

## Gate.io BTC 合约交易机器人 (gate_bot_gui v14)

### 项目路径
- Windows: `C:\Users\Administrator\Desktop\gate_bot_gui\`
- 工作空间镜像: `/home/node/.openclaw/workspace/`
- 主要文件: `src/engine.js`, `src/index.html`, `main.js`

### 技术栈
- Electron + Node.js
- `nodeIntegration: true`, `contextIsolation: false`（无 preload.js）
- `const { ipcRenderer } = require('electron')` 写在 index.html 顶部

### 策略核心
- 5分钟K线信号 + 趋势过滤（1h/30m/auto ADX自适应）
- EMA9/21金叉、布林带突破、顺势趋势跟随
- 动态ATR止损止盈（1.5x ATR，2:1 RR）
- 分批止盈：30%@tp1(3%), 30%@tp2(6%), 40%移动止损
- 评分阈值 ≥60 才开仓
- 60秒扫描间隔，止损后6槽(30min)冷却

### API 状态
- ✅ 公开端点: `/futures/usdt/tickers`, `/futures/usdt/candlesticks`
- ✅ 私有POST: `/futures/usdt/positions/BTC_USDT/leverage`
- ❌ 私有GET: `/futures/usdt/accounts` — 持续返回 `INVALID_SIGNATURE`
- API Key: `2b0b3bc5704916d95676b6e12288552c`
- 签名算法: HMAC-SHA512，signStr = `METHOD\n/api/v4PATH\nQUERY\nBODY_HASH\nTIMESTAMP`

### 账户状态
- 真实余额: 0.71 USDT（合约账户，不足以实际交易，最低需10-20 USDT）
- 模拟账户: 10,000 USDT（dryRun: true）
- 模拟状态持久化: `sim_state.json` in `app.getPath("userData")`

### 模拟交易结果（2026-03-17）
- Trade 1: LONG @ 74385.90 → TP → +92.53U
- Trade 2: LONG @ 75172.00 → TP → +88.19U
- Trade 3: LONG @ 75754.10 → SL → -72.26U
- 累计净利: +108.47U

### 已修复的Bug清单
1. `ipcRenderer` 访问问题 → `nodeIntegration: true`, `contextIsolation: false`
2. `getPrice is not a function` → 方法定义问题修复
3. 持仓浮动盈亏不更新 → 在 dryRun 分支中加 emit("position", ...)
4. 平仓后余额不更新 → 在 return 前加 emit("balance", ...)
5. `feeOpen/feeClose` NaN → 开仓时计算后存入 this.pos
6. notional 计算错误 → `notional = contracts * price * mult`（不乘杠杆）
7. `signStr` 变量提升错误 → ts 先定义再用
8. 启动阻塞 → 汇率获取用 setTimeout 异步化
9. 倒计时改为5分钟(300s)

### 今日代码审查发现的Bug（尚待用户修复）
1. **engine.js**: `return` 语句后有死代码（emit balance 在 return 后）→ 已在精简版中修复，移到 return 前
2. **engine.js**: `this.pos` 创建时缺少 `feeOpen/feeClose` 字段 → 修复
3. **engine.js**: `clearSimState()` 逻辑 `if (this.simFile) return` 应为 `if (!this.simFile) return` → 修复
4. **engine.js**: `require("electron").remote` 已废弃 → 改用 try/catch require("electron")
5. **index.html**: `setTrendMode` 函数定义了两次 → 删除重复
6. **index.html**: `updateBalanceDisplay` 定义在 if 块内部 → 移到全局
7. **index.html**: `config:loaded` 监听两次 → 删除重复
8. **index.html**: position 事件中用了未定义变量 `entryStr/slStr/tpStr` → 修复
9. **index.html**: 引用了不存在的 DOM 元素 `s-total-label`, `s-total`, `margin-card`, `s-margin`

### 配置系统
- 多profile支持：文件对话框保存/加载 JSON
- IPC: `config:saveDialog`, `config:loadDialog`, `config:saved`, `config:loaded`
- API密钥持久化: localStorage (`gate_api_key`, `gate_api_secret`)

### 货币显示
- USDT / CNY 切换
- 汇率来源: CoinGecko API（每小时更新）
- localStorage 保存偏好: `currencyMode`

### 用户信息
- 操作系统: Windows
- 响应语言: 中文
- 偏好: 保守风险控制，5x杠杆，20%仓位比例

### 关键配置常量
```
DEFAULTS = { capital:500, takePct:3, stopPct:1.5, leverage:5, atrMult:2,
             tp1Pct:3, tp2Pct:6, trailPct:1, volMult:1.2, posRatio:20, maxLoss:15 }
```

### Gateclaw-api 元数据（勿修改）
```json
{ "label": "gateclaw-api (gateway-client)", "id": "gateway-client",
  "name": "gateclaw-api", "username": "gateclaw-api" }
```
