# 2026-03-15 - Gate.io BTC 交易机器人代码清理

## 会话摘要
帮助用户清理 Gate.io BTC 交易机器人（Electron + Node.js）的代码，修复因多次修改导致的重复代码问题。

## 已完成的工作

### 代码结构修复
- 用户发送了三个文件：`engine.js`、`index.html`、`main.js`
- 主要问题：`index.html` 第 488 行之后有大量重复的 JavaScript 函数定义
  - `toggleBot()`、`startBot()`、`stopBot()` 等函数被定义了 2-3 次
  - `getConfig()` 重复定义
  - IPC 事件监听器重复绑定
  - 导致按钮点击无反应、启动失败

### 清理方案
- 确定替换范围：第 488 行到第 835 行（从 `// 页面加载时读取保存的 API` 到 `</script>` 之前）
- 提供完整的清理后代码，包含：
  - 单一版本的 `toggleBot()` / `startBot()` / `stopBot()`
  - 单一版本的 `getConfig()` 和 `handleBotStopped()`
  - 清理后的 IPC 事件监听（`bot:started`, `bot:stopped`, `bot:event` 等）
  - 保留所有功能：趋势模式切换、API Key 本地存储、图表交互等

### 已知问题（用户决定稍后处理）
- API 签名验证失败（`INVALID_SIGNATURE` / `Signature mismatch`）
- 合约账户 USDT 余额查询返回 0 或失败
- 用户表示："目前只有 API无法获取合约账户内的USDT 这个问题，以后再处理"

## 关键文件路径
- 工作目录：`C:\Users\Administrator\Desktop\gate_bot_gui\`
- 主要文件：`src\engine.js`、`src\index.html`、`main.js`

## 技术细节
- 使用 SHA512 签名算法（Gate.io API v4 要求）
- 已实现三种趋势模式：1h保守、30m激进、ADX自适应
- 已实现动态 ATR-based 止盈止损
- 已实现分批止盈（30% at 3%, 30% at 6%）
- 已实现移动止损保护

## 下一步（由用户决定）
1. 应用 index.html 的代码清理
2. 测试机器人启动/停止功能
3. （可选）后续处理 API 签名和余额查询问题
