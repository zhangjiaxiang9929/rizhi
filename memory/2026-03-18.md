# 2026-03-18 工作记录

## 用户信息
- 用户通过 webchat 联系，使用中文交流
- 正在开发 BTC 合约交易机器人（Electron + Node.js）
- 交易所：Gate.io BTC_USDT 永续合约

## 项目结构
- 路径：`D:\gate bot\src\`
- 文件：`engine.js`、`index.html`、`main.js`、`ai-advisor.js`（新建）
- `package.json` 在 `D:\gate bot\` 根目录

## 今日完成的工作

### 1. AI 集成
- 新建 `ai-advisor.js`，接入 DeepSeek AI 做开仓前二次确认
- AI API：`api2.openclawcn.net/v1/chat/completions`
- API Key：`sk-c8ltKB9BbxxMteuEr0xja9N6O3uD68ykmTggXEcEPMsulvM2`
- 模型：`deepseek-v3.2阿里云`
- 逻辑：策略 score>=50 + AI置信度>=65 才开仓，超时/失败不开仓

### 2. UI 改进
- 底部新增「🤖 AI对话」Tab
- 快捷问题按钮 × 4
- AI回复支持 Markdown 渲染（renderMarkdown 函数）
- AI对话支持多轮历史（最多10轮）
- 打字动画效果

### 3. 数据接入
- 订单簿深度（`getOrderBook`）
- 资金费率（`getFundingRate`）
- 多空比（`getLongShortRatio`）
- 持仓量变化（`getOpenInterest`）
- 信号栏显示4个数据徽章

### 4. AI 持仓巡检（设计完成，待实现）
- 持仓中每5分钟AI巡检一次
- AI可执行：hold / reduce(减仓50%) / close(全平)
- 新增 `aiCheckPosition` 函数
- `engine.js` 持仓管理部分加入巡检逻辑

### 5. 策略优化
- EMA三线排列确认（需 EMA9>EMA21>EMA55 才触发顺势信号）
- 去掉 MTF 限制，交给 AI 判断顺逆势
- 动态冷却期（亏损越大冷却越久）
- 默认止盈提高（tp1: 3%→5%, tp2: 6%→10%）

## 遇到的问题
- `<br>` 字符在聊天界面频繁被截断，改用 `.split("\n").join("<" + "br" + ">")` 解决
- AI 对话框 `aiTyping` 变量初始化问题，改用 `var` 声明解决
- `ai-advisor.js` 中文引号导致语法错误，改用数组 join 方式构建 prompt

## 待完成
- 持仓巡检模块实际加入 `engine.js`
- 多空比/持仓量数据传给 AI
- 模拟模式测试验证
- 确认 `toggleBot` / `saveConfigDialog` 函数是否丢失（大段替换时可能误删）

## 技术细节
- OpenClaw 内部 API endpoint：`gateclaw-api-internal.gate-claw.svc.cluster.local:9090/internal/ai/v1`
- 可用模型包括：`deepseek-v3.2阿里云`、`Qwen3-Max`、`kimi-k2.5阿里云` 等
- Gate.io contract_stats API 提供多空比和持仓量数据
- 手续费：开平各0.05%，合计0.1%，计算用 `sz * lev * 0.0005 * 2`

## 下午新增工作（15:04）

### 6. AI主导模式开关
- `index.html` 模拟模式下方新增「🤖 AI主导模式」toggle
- `engine.js` 新增 `runAiDriven()` 方法，AI自主决定开仓方向
- `ai-advisor.js` buildPrompt 加入 aiDrivenMode 三元判断
- `getConfig()` 加入 `aiDriven` 字段

### 7. 参数面板折叠
- 交易参数 card 改为「常用参数始终显示 + 高级参数折叠」
- 新增 `toggleAdvanced()` 函数
- 高级参数：ATR倍数、第一止盈%、第二止盈%、移动止损、成交量倍数

### 8. 交易预期计算器重设计
- 改为网格布局，每行清楚显示：账户资金/实际投入/名义仓位/止盈/止损/手续费/净盈亏/盈亏比/最低胜率/月均预期
- 修复手续费计算（原来用0.0007，改为0.0005）
- 修复 `avgPer` 重复声明导致的报错

### 9. AI对话框 system prompt 修改
- 加入「只分析不执行交易」限制，防止AI模拟下单回复
- `getMarketContext()` 加入机器人未启动判断，防止误报持仓

## 当前问题
- `toggleBot` / `saveConfigDialog` is not defined 错误 — 疑似大段替换时误删函数体
- `avgPer has already been declared` — 已定位，需删除重复声明
