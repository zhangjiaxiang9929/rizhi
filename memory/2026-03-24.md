# 2026-03-24 记忆日志

## 交易机器人状态（UTC 00:00–00:24）

### 当前持仓
- 第24笔：LONG @ 70,914.00，止损 70,346.69，止盈 72,190.45
- 参数显示仍为旧的 **1.0%/2.8%**（engine.js 第360-361行修改未确认生效）
- 截至最后日志，价格在 70,700–70,800 区间震荡，浮亏约 -0.8~-1.8U

### 交易历史（23笔，截至 03:46）
- 总盈亏：**+6.41U**
- 胜率：56.5%（13/23）
- LONG 5笔：全胜，+5.58U
- SHORT 18笔：44%胜率，+1.07U

### 账户余额
- 启动余额：1001.01U（手动修正后）
- 第22笔止盈后：1004.85U
- 第23笔 AI平仓 -0.18U 后：约 1004.68U

### 关键问题
1. **大单参数疑似未生效** — 日志第23笔还显示 `1.0%/2.8%固定标准`，需检查 engine.js 360-361 行是否真的保存了 `fixedSLob = price * 0.008` / `fixedTPob = price * 0.018`
2. 第24笔止损 70,346，价格在 70,600–70,800，尚未触发

### 待办事项（来自上个会话）
- [ ] Fix 2b：runAiDriven() AI SL 0.003–0.03 / TP 0.008–0.08
- [ ] Fix 3：run() AI巡检 Promise.all 加入 lsRatio + openInterest
- [ ] Fix 4：删除 emit("trade") 后重复的连亏计数 block
- [ ] 确认大单参数 0.8%/1.8% 是否真正生效
- [ ] 积累至约50笔后分析多空胜率，考虑仅做 LONG

### 策略方向共识
- SHORT 胜率持续偏低（44%），LONG 100% 胜率
- 方向：**考虑屏蔽 SHORT 信号，仅做 LONG**（待50笔数据确认后决策）
- 大单策略参数目标：SL 0.8%，TP 1.8%，R:R = 2.25:1
- AI巡检频繁超时（凌晨4点），属正常现象

---

## 补充记录（UTC 01:26–01:41）

### 用户发来了完整的 engine.js（最新运行版本）

这是真正运行中的版本，与工作区 `/home/node/.openclaw/workspace/engine.js` 不同。
用户将 engine.js 作为附件发送，表明实际文件不在工作区，工作区的 engine.js 是旧的测试版。

### 确认已有功能（用户发来的版本）
- ✅ `aiCheckPosition` 已引入：`const { aiAdvise, aiCheckPosition } = require("./ai-advisor");`
- ✅ AI 巡检逻辑已存在于 `run()` 持仓分支（checkInterval=120000, Promise.all 含 lsCheck/oiCheck）
- ✅ Fix 2b 已完成：`runAiDriven()` 中 aiSLValid2/aiTPValid2 范围已是 0.003–0.03 / 0.008–0.08
- ✅ Fix 3 已完成：`run()` AI巡检 Promise.all 已包含 `getLongShortRatio()` 和 `getOpenInterest()`
- ✅ 大单参数已是 0.8%/1.8%（fixedSLob = price * 0.008, fixedTPob = price * 0.018）
- ✅ 日志字符串也已更新为 `0.8%/1.8%固定标准`

### 本次会话完成的修改
- **本地兜底逻辑**（已给出代码，用户需手动插入）：
  - 在 `run()` 持仓分支 AI巡检结果处理里，`if (checkResult.action === "close")` 前插入：
  ```javascript
  if (!checkResult || checkResult.reason === "巡检失败") {
      const holdMinutesFb = Math.floor((Date.now() - this.pos.time) / 60000);
      const netPnlFb = this.pos.notional * pPct / 100 - (this.pos.feeOpen || 0) - (this.pos.feeClose || 0);
      if (holdMinutesFb >= 150 && netPnlFb < 0) {
          this.log("warn", `⏰ 本地兜底：持仓${holdMinutesFb}分钟且净亏损，强制平仓`);
          checkResult = { action: "close", reason: "超时横盘，浮亏持续" };
      }
  }
  ```
  - 触发条件：AI巡检失败 AND 持仓 ≥ 150分钟 AND 净亏损
  - **注意**：工作区 engine.js 与用户实际运行版本不同，edit 工具无法直接修改，需用户自行粘贴

### 待办事项更新
- [x] Fix 2b：已确认在用户版本中完成
- [x] Fix 3：已确认在用户版本中完成
- [ ] Fix 4：删除 `startPriceTicker()` hitSL 分支里 `emit("trade")` 后的重复连亏计数 block（代码见下）
  ```javascript
  // 需删除的重复块（startPriceTicker 里 emit("trade") 之后）：
  if (pnl >= 0) {
    this.consecutiveLosses = 0;
  } else {
    this.consecutiveLosses = (this.consecutiveLosses || 0) + 1;
    this.log("warn", `📊 连亏计数: ${this.consecutiveLosses}次`);
  }
  ```
  注：用户发来的 engine.js 已无此重复块，可能已修复，下次确认
- [ ] 本地兜底逻辑：已给出代码，等待用户插入

### 版本管理注意
- 用户实际运行的 engine.js 比工作区更新；以后若需修改，请用户直接发送附件或告知实际路径
- 用户本次 SHORT 胜率统计更新：LONG 3/5=60%，SHORT 5/18=28%；均低于上次笔记的估计，需继续积累

---

## 05:56 UTC — 用户上传最新版本代码（重要：以此为准）

### 版本说明
用户明确指出之前的回答基于旧版 Gate 技能文档，不是用户实际的交易机器人代码。
**以下是用户实际运行的最新版本**，后续所有回答必须基于这4个文件。

### 文件路径（用户本地）
- `D:\AI 量化机器人\src\engine.js`
- `D:\AI 量化机器人\src\ai-advisor.js`
- `D:\AI 量化机器人\src\index.html`
- `D:\AI 量化机器人\src\main.js`

### 开仓策略（以代码为准）

#### 路径一：策略信号 + AI过滤（run()）
- 5m K线信号：EMA交叉 / 布林带突破 / EMA排列 / EMA反弹
- ATR过滤：atr/close < 0.0003 → HOLD（非常宽松）
- 评分阈值：score >= 45（连亏3次+提升到60）
- 趋势过滤：EMA差值 >= 0.15% 才算有方向（LONG/SHORT/HOLD）
- AI置信度：>= 58 才允许开仓（连亏3次+提升到68）
- 止损止盈（动态，按5m EMA差值）：
  - EMA差 > 0.6%（强）→ 止损2% 止盈7%
  - EMA差 > 0.3%（中）→ 止损1.5% 止盈5%
  - EMA差 < 0.3%（弱）→ 止损1% 止盈3.5%
  - AI建议止损/止盈在有效范围内（SL: 0.3%-3%，TP: 0.8%-8%）且盈亏比>=1.5时优先使用

#### 路径二：大单信号（startOrderBookWatcher()）
- 触发条件：买方从<45%跳到>75% 且 成交量放大2倍
- 趋势过滤：与当前trendMode一致（动态判断30m/1h/auto）
- 固定止损：0.8%，固定止盈：1.8%，盈亏比2.25
- 仓位：主策略的40%
- AI主导模式开启时禁用

#### 路径三：AI主导模式（runAiDriven()）
- 每5分钟槽自动触发，AI自主判断方向和仓位
- AI置信度阈值：>= 62（连亏3次+提升到68）
- 1h趋势与AI方向相反：仅记录警告，不强制拒绝
- AI动态仓位：代码里 `const aiRatio = 1.0`（固定100%，未启用动态）
- 止损止盈：按5m EMA差值动态计算（同路径一），优先用AI建议值

### 不开仓条件（以代码为准）
1. 趋势不明朗（EMA差 < 0.15%，输出HOLD）
2. 在冷却期（止损后）
3. 在重要时间窗口（UTC 12/14/08点前后10分钟）
4. 每日盈亏触限（日盈>=3% 或 日亏>=2%）
5. 异常波动保护中（1分钟涨跌>1%，暂停10分钟）
6. AI置信度不足
7. 连续同方向信号3次以上（震荡行情保护）

### main.js 已知Bug（不影响功能）
- 第21行 `if (engine) engine.` 是一个语法残留，会导致启动报错
- 用户需删除这行：`if (engine) engine.`

### AI模型
- 交易决策AI：`qwen3-max-2026-01-23阿里云特价`
- 对话AI：`qwen3-max-2026-01-23阿里云特价`
- API：`api2.openclawcn.net`


---

## 05:48–05:58 UTC — 开仓规则问题 + 最新代码接收

### 用户问题
用户上传了BTC K线截图（下跌趋势中的横盘），询问当前开仓规则以及这种趋势是否不开仓。

### 重要纠正
之前回答错误引用了系统内置的 Gate 技能文档，用户明确指出这不是他的策略。
**以后必须只参考用户上传的4个源文件，不能引用 gate-exchange-futures/spot skill 文档。**

### 最新代码确认（05:56 UTC 用户上传）
项目路径：`D:\AI 量化机器人\src\`
文件：engine.js / ai-advisor.js / index.html / main.js

#### 开仓路径总结（以代码为准）

**路径一：策略信号+AI过滤（run()）**
- 5m信号：EMA交叉/布林带/EMA排列/反弹
- ATR阈值：atr/close < 0.0003 → HOLD
- 评分：>= 45（连亏3次+升至60）
- 趋势：EMA差 >= 0.15% 才有方向
- AI置信度：>= 58（连亏3次+升至68）
- 动态止损止盈：EMA差>0.6%→止损2%止盈7%，>0.3%→1.5%/5%，其余→1%/3.5%

**路径二：大单信号（startOrderBookWatcher()）**
- 买方从<45%跳>75%且成交量2倍
- 趋势动态过滤（跟随trendMode）
- 固定止损0.8%/止盈1.8%，仓位40%
- AI主导模式时禁用

**路径三：AI主导模式（runAiDriven()）**
- 每5分钟槽触发，AI自主判断方向
- 置信度>=62（连亏3次+升至68）
- 1h趋势相反→警告不强制拒绝
- aiRatio固定1.0（未启用动态仓位）

#### 不开仓条件
1. 趋势HOLD（EMA差<0.15%）
2. 冷却期中
3. 重要时间窗口（UTC 12/14/08点±10min）
4. 日盈>=3% 或 日亏>=2%
5. 异常波动保护（1min涨跌>1%）
6. AI置信度不足
7. 同方向信号连续3次（震荡保护）

#### main.js 已知Bug
第21行 `if (engine) engine.` 是不完整语句，会导致启动报错，需删除。

### AI配置
- 模型：`qwen3-max-2026-01-23阿里云特价`
- API：`api2.openclawcn.net`
- Key：`sk-c8ltKB9BbxxMteuEr0xja9N6O3uD68ykmTggXEcEPMsulvM2`

