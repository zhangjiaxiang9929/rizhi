# 2026-03-30 Memory Log

## BTC Bot Session — Mar 30 UTC 00:00–01:10

### Market Overview
- 00:00 UTC: BTC ~66480, oscillation mode detected (⬜ 震荡模式)
- 04:19 UTC: BTC surged to 66724 (RSI touched 72.3 briefly → oscillation mode watching)
- 06:41–06:50 UTC: **MAJOR CRASH** — BTC 66190 → 64973 (−1217点! RSI=11, extreme oversold)
- Post-crash recovery: 65000 → 66700+ by 08:25 UTC
- End of session: BTC ~66260

### Oscillation Mode Behavior (First Extended Test)
- 04:25–06:00 UTC: Dozens of scans showing `⬜ 震荡模式 | RSI:XX 未达反转条件`
- RSI stayed in 32–72 range throughout → no oscillation entry triggered
- **Confirmed**: oscillation detection logic works correctly; RSI threshold properly enforced

### Trades Executed Today (3 trades)

**Trade 1: SHORT @ 65908.9 (07:00 UTC) — LOSS**
- Trigger: AI timeout (12s) + score=75 + EMA差=0.424% → bypass 缩仓50%, 79张
- Context: BTC had just crashed to 64973 and was REBOUNDING to 65908 when entry fired
- Dynamic SL: triggered immediately at 07:01 UTC
- Result: **-3.80U** (direction wrong — shorting during oversold bounce)
- Root cause: EMA差 still showed strong SHORT after crash, but price was already bouncing from RSI=11

**Trade 2: SHORT @ 66033.2 (07:25 UTC) — NEAR BREAKEVEN**
- Trigger: score=78, AI confidence 55%<60% → bypass 缩仓50%, 79张, 弱趋势 SL1.5%/TP2.8%
- 分阶段动态止损 FIRST REAL TRIGGER: 08:05 UTC, 浮盈0.36% → 止损移至保本 66033.2 ✅
- Price moved as expected to 65780 (浮盈+1.95U) then bounced back through 保本线
- Result: **-0.27U** (保本 basically, just fees)

**Trade 3: SHORT @ 66607.0 (08:25 UTC) — PROFIT, MANUAL CLOSE**
- Trigger: 连续3次SHORT + EMA差0.122%>0.1% → 趋势突破; AI置信55% → bypass 缩仓50%, 78张
- Dynamic SL worked perfectly: 66835 → 66821 → 66607 (保本) → 66589 → 66546
- 阶段1触发: 08:35 UTC, 浮盈0.35% → 止损移至保本 66607 ✅
- Peak: +2.79U (浮盈0.54% at 08:40 UTC, price 66249)
- **User manually closed at 08:41 UTC** @ ~66250, net profit ~+1.73U
- After close: price continued to 66230 (could have been more)
- Result: **~+1.35U** (after fees)

**Day total: -2.45U** (日志confirmed)
**Cumulative account: ~994U** (996.52 - 2.45 ≈ 994.07U)

### Key Strategy Validations (First Real Tests)
1. ✅ **分阶段动态止损** — triggered correctly in both Trade 2 & 3 (stage 1 breakeven move)
2. ✅ **Bypass逻辑 (AI timeout / low confidence)** — fired in all 3 trades
3. ✅ **震荡模式检测** — correctly stayed in standby (RSI never hit <32 or >68 thresholds during oscillation window)
4. ✅ **连续信号EMA差过滤** — Trade 3 correctly waited for 3x consecutive + EMA>0.1%

### New Risk Identified: Post-Crash Oversold Bounce Problem
- **Issue**: After acute crash (RSI=11), EMA差 still shows strong SHORT signal
- **But**: Price is already bottomed and bouncing rapidly
- **Bypass logic fires** (AI timeout + strong score + EMA diff large) → opens SHORT into bounce
- **Trade 1 (-3.80U) is direct result of this**
- **Proposed fix**: Add "RSI<20时不做空" (no SHORT when RSI<20, extreme oversold)
- **User asked if we should add this** — not yet implemented as of end of session

### Oscillation Mode — aiAdviseOscillation Still Not Triggered in Live Trading
- Has only been in trend path so far
- First oscillation path trigger was observed at 02:03 UTC Mar 30 (from pre-compaction summary)
- But EMA差 was sufficient for trend path, so trend logic ran
- True test: need `trend1h === "HOLD"` + `marketMode === "oscillation"` + RSI<32 or >68

### Strategy Behavior Notes
- Low-liquidity filter (UTC 22:00/23:00/0:00/1:00) correctly blocked scans during crash window
- Strategy correctly identified 趋势模式 switch at 06:45 UTC when crash EMA diff blew out
- `margin is not defined` warning still occasionally appearing (non-critical, HOLD path)

### Next Actions Discussed
- [ ] Add RSI<20 no-SHORT protection (post-crash oversold bounce guard)
- [ ] Monitor first true oscillation AI call in live trading
- [ ] User manually closes positions — consider if auto TP at 0.5% would be better for volatile days

### Files Modified This Session
- No code changes uploaded (all analysis/monitoring session)
- workspace: `/home/node/.openclaw/workspace/btc_bot/`
- Gist: `58ea1bcfc6058dba168adbaa651f86d7`
- Latest uploads: `engine.js` + `ai-advisor.js` @ `2026-03-29T18:01:09Z`, `index.html` @ `2026-03-29T17:55:47Z`

---

## Session Update — Mar 30 01:18–01:37 UTC

### Code Changes Implemented & Uploaded

**Change 1: RSI Extreme Value Protection (engine.js)**
- Added before the `if (sig !== "HOLD" && score >= 45)` block opens AI logic
- `RSI < 20` → skip SHORT (防止急跌后追空反弹) — directly prevents Trade 1 (-3.80U) scenario
- `RSI > 80` → skip LONG (顺手加上, 防止急涨后追多回调)
- Log message: `⛔ RSI{x}<20 极度超卖，禁止做空（防止追空急跌反弹）`
- engine.js uploaded: `2026-03-30T01:18:19Z`
- Raw URL: `https://gist.githubusercontent.com/zhangjiaxiang9929/58ea1bcfc6058dba168adbaa651f86d7/raw/2d7817133794877db059ed86bf049a3c49e82e76/engine.js`

**Change 2: Trade History Shows Open + Close Time (engine.js + index.html)**
- `this.pos` now includes `openTime: Date.now()` field at position open
- Both `tradeHistory.push()` calls (sim ~line 331, real/manual ~line 1868) now include `openTime` and `closeTime`
- `index.html` table header split: "时间" → "开仓时间" + "平仓时间" (9 columns total, colspan updated to 9)
- `renderHistory()` uses `fmtTime()` helper; old records with no `openTime` show `--`
- index.html uploaded: `2026-03-30T01:18:19Z`

**Change 3: Parameter Auto-Save via localStorage (index.html only)**
- Root cause: existing `applyConfigLive()` sent params to engine but never persisted to localStorage
- Fix: `applyConfigLive()` now also saves all 12 params to `localStorage` with `cfg_` prefix
- `setTrendMode()` now saves `cfg_trendMode` to localStorage
- `onDryRunToggle()` now saves `cfg_dryRun` to localStorage
- `DOMContentLoaded` now restores all params from localStorage before other init
- First restart still shows defaults; after first adjustment, all future restarts preserve settings
- index.html uploaded: `2026-03-30T01:30:20Z`
- Raw URL: `https://gist.githubusercontent.com/zhangjiaxiang9929/58ea1bcfc6058dba168adbaa651f86d7/raw/148d4b533327f286f56c841b7da62d04ef57ebe5/index.html`

### User Context
- User restarted bot after downloading new engine.js/index.html (RSI protection + open/close time)
- Bot restarted at 09:20:53 UTC+8 (01:20 UTC) with default params: 止盈2.8% 止损1.5% 3x 仓位**20%**
- User noted param reset was a bug from code update — confirmed root cause (no localStorage save)
- User setting before: 仓位35%, 30m激进 mode; reverted to 20%, 保守模式 after restart
- After localStorage fix, params will persist across restarts automatically

### Bot Status at Session End
- Running in 模拟模式, 保守1h filter
- Account: 994.07U (994.89U available per log)
- Low-liquidity block active (UTC 1:00)
- First scan: LONG 63pts, AI rejected 58% confidence (1h downtrend)
- BTC price at restart: ~66469

### Latest Gist Upload Summary
- `engine.js`: `2026-03-30T01:18:19Z` (RSI<20 no-SHORT + RSI>80 no-LONG; openTime in pos; openTime/closeTime in tradeHistory)
- `index.html`: `2026-03-30T01:30:20Z` (open+close time columns; localStorage param persistence; all above)

---

## Session Update — Mar 30 04:18–04:48 UTC

### Bug Fix: JS String Literal Syntax Error in `index.html`
- **Issue**: User reported a syntax error (red squiggle in editor) in the AI UI code of `index.html`.
- **Root Cause**: The string literal inside the `replace` function in `appendAiMsg()` contained an unescaped raw newline instead of `\n` or `<br>`.
  ```javascript
  // The failing code sent by user:
  div.innerHTML = `<div class="chat-avatar ${cls}">${avatar}</div>
  <div class="chat-bubble">${content.replace(/\n/g,"
  ").replace(/\*\*(.+?)\*\*/g,"<b>$1</b>")}</div>`;
  ```
- **Resolution**: Replaced the raw newline with `"<br>"` in the `.replace()` string to properly render markdown line breaks as HTML without breaking syntax. Provided full replacement function.
- Note: There was a brief confusion where AI misdiagnosed a similar error in `appendAITyping()`, but then correctly homed in on the multiline string literal error provided by user.