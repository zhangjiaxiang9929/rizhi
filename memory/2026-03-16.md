# Memory - March 16, 2026

## Gate.io BTC Trading Bot v14 - UI Structure Issues

### Current Problems (In Progress)
- **index.html structure corrupted** after adding CNY currency toggle feature
- K-line chart (`<canvas id="price-chart">`) not rendering - blank area
- Math expectations section misplaced/missing
- BTC real-time price not displaying
- Arrow position wrong
- JavaScript errors causing entire UI to malfunction

### Root Causes Identified
1. `rateDisplay` element was deleted from HTML but JavaScript still references it:
   - `document.getElementById("rateDisplay").textContent = exchangeRate.toFixed(2);`
   - This causes script crash, breaking all functionality

2. Duplicate position handling code in `ipcRenderer.on("bot:event")` handler

3. Nested `<div class="form-row">` elements (duplicate nesting)

4. Missing `updateBalanceDisplay()` function that `refreshAllMoneyDisplays()` tries to call

### Fixes Required
1. Remove or comment out `document.getElementById("rateDisplay")` reference in `updateCurrency()` function
2. Remove duplicate nested `form-row` div around currency selector
3. Remove duplicate position handling code block (~lines 2230-2244)
4. Add missing `updateBalanceDisplay()` function
5. Ensure `window.lastBalanceData` is used properly for currency refresh

### CNY Feature Status
- Currency toggle (USDT/CNY) implemented but causing structural issues
- Exchange rate fetched automatically from CoinGecko API in engine.js
- Format functions `formatMoney()` and `formatMoneyCompact()` exist
- Need to ensure UI elements exist before JavaScript tries to access them

### File Locations
- `C:\Users\Administrator\Desktop\gate_bot_gui\src\index.html` - UI (corrupted)
- `C:\Users\Administrator\Desktop\gate_bot_gui\src\engine.js` - Trading engine

### Next Steps
1. Fix JavaScript errors by removing missing element references
2. Clean up duplicate code
3. Verify K-line chart renders after fixes
4. Ensure math expectations display correctly
5. Verify BTC price display works
6. Test CNY toggle functionality

### Key Code Sections to Check
- `updateCurrency()` function - remove rateDisplay reference
- `ipcRenderer.on("bot:event")` - remove duplicate position handling
- Currency selector HTML - remove nested form-row
- `refreshAllMoneyDisplays()` - add updateBalanceDisplay function

---
Session: Pre-compaction flush, March 16, 2026 16:25 UTC
