# Memory — 2026-03-13

## BTC Bot (Gate.io Electron App)

- Project path: `/home/node/.openclaw/workspace/btc-bot/`
- Three-file app: `engine.js`, `main.js`, `index.html`
- Current version: **v12** (all bug fixes applied, Y-axis price labels)

### Bug fixes delivered today
1. `engine.js`: Trailing stop — never destructure `sl` from `this.pos`; read `this.pos.sl` directly to avoid stale values
2. `engine.js`: `partialClose` — use `this.pos.contracts` in dryRun mode (not `livePos.size`)
3. `engine.js`: Removed unused `prev4h` variable
4. `index.html`: Replaced `ipcRenderer.emit("bot:stopped")` with direct call to `handleBotStopped()` function
5. `index.html`: Fixed `toX` denominator — use `priceHistory.length - 1` (not `MAX_PTS - 1`) to spread points correctly
6. `index.html`: Added Y-axis price labels to price chart — max (green), current (orange), min (red) on right side; `PAD_R = 64`; anti-overlap filter (min 13px gap)

### Key architecture notes
- `handleBotStopped()` must be defined before `ipcRenderer.on("bot:stopped", handleBotStopped)` registration
- `_setConn()` in engine.js only emits on state change (prevents log spam)
- Trade records use `result: "open"` for open entries; filtered from closed-trade stats
- History: capped at 500 on disk, 200 sent to renderer; synchronous I/O in main.js
- CSV export saves to Downloads folder with UTF-8 BOM (Excel Chinese compat)
- API Secret never saved to disk

### Status
- v12 delivered to user; awaiting run confirmation
- User confirmed v11 ran successfully

---

## BTC Bot — v13 / v13.1 Updates (afternoon session)

### v13 index.html delivered
- Layout: `body { display:flex; flex-direction:column }`, `#app { display:flex }`, `#left-toggle` as flex sibling between panels (NOT position:absolute)
- Price chart height: 68px → 88px
- Global font sizes increased ~1-2px
- Log line-height: 2, font-size: 12px
- Stats bar svalue font: 16px
- Signal row: MTF badge + score bar (hidden when HOLD/score=0)
- Position card: 5 columns (added 持仓张数)
- Trailing stop indicator badge
- Score bar: <50 red / 50–70 orange / ≥70 green, width capped at 70px

### v13 engine.js — no changes from v12 except:
- Added `"1m":60` to `SECS` lookup in `fetchKlines()`
- `start()` now pre-loads 120 x 1m klines on startup and emits `priceHistory` event
- Chart fills immediately on start instead of waiting for live ticks

### v13 index.html — priceHistory handling
- `bot:event` type `"priceHistory"` → batch-fills `priceHistory[]` array and redraws chart
- Logs "📊 历史价格已加载 N 个数据点"

### main.js — no changes needed (same as v12)

### Key decisions
- `write()` tool fails with missing `content` validation error — always deliver full file code in chat
- `ipcRenderer.emit` → direct function call pattern confirmed working
- GPU cache errors in CMD are harmless Chromium issues (拒绝访问 0x5), not code bugs
- User confirmed v13 renders correctly in fullscreen ("启动正常" equivalent)

---

## BTC Bot — v14 Final + Optimization Session (13:00–13:30 UTC)

### v14 complete files written to workspace
- `engine.js` written: `/home/node/.openclaw/workspace/btc-bot/engine.js`
- `main.js` written: `/home/node/.openclaw/workspace/btc-bot/main.js`
- `index.html` delivered in chat (too large to write via tool)

### Bug fixes & optimizations applied in v14 final
1. **engine.js**: `run()` no longer emits `price` event (priceTimer handles it, avoiding duplicate emits)
2. **engine.js**: Unified entry price to `this.pos.entry` everywhere (avoids livePos slippage discrepancy in real trading)
3. **engine.js**: `scoreSignal` — SHORT RSI scoring fixed: only adds points when RSI > 50 (was incorrectly rewarding RSI > 40 for shorts)
4. **engine.js**: Added `cooldownUntilSlot` — after a stop-loss, bot waits 2 × 15min slots (30 min) before opening new position. Logs "⏸ 冷却期 剩余约Xmin"
5. **engine.js**: Signal emit now uses `trend1h` field (was `trend4h` — naming cleanup)
6. **main.js**: `_tmpEngine` singleton — reuses single TradingEngine instance for `price:get` and `chart:loadKlines` instead of creating new instance every 2 seconds
7. **main.js**: `_tmpEngine` cleared to null when bot starts (engine takes over)
8. **index.html**: Chart height 100px → 150px (better candlestick visibility)
9. **index.html**: `#cool-badge` added to signal-row — shows "⏸ 冷却Xmin" during cooldown period
10. **index.html**: MTF badge now shows direction: `✅ 1h→LONG` or `⚠ 1h→SHORT`
11. **index.html**: `updateMath()` now shows monthly expectation estimate (50% winrate × 30 trades/month)
12. **index.html**: Left panel scrollbar styled (`width:4px`, custom thumb color)
13. **index.html**: `↺ 恢复默认` button added and working

### Strategy confirmed (v14)
- 15m signal + 1h trend filter
- Score threshold: 65
- ATR filter: 0.0015
- EMA touch margin: 0.3%
- Default params: takePct=3%, stopPct=1.5%, leverage=5x, posRatio=20%, maxLoss=15%
- Auto breakeven: when profit ≥ stopPct, move SL to entry price
- Cooldown: 30min after any stop-loss

### IPC channels reference (complete)
- Handles: `chart:loadKlines`, `price:get`
- On: `bot:start`, `bot:stop`, `bot:updateConfig`, `trade:partialClose`, `trade:setTP`, `trade:setSL`, `history:load`, `history:clear`, `history:exportCSV`, `config:load`, `config:save`
- Send to renderer: `bot:started`, `bot:stopped`, `bot:event`, `trade:error`, `history:updated`, `history:exported`, `config:loaded`, `config:saved`
- `bot:event` types: `log | conn | price | balance | signal | position | trade | stopped | realBalance | configUpdated | priceHistory`

### User status
- Files delivered, user copies manually to `C:\Users\Administrator\Desktop\gate_bot_gui\src\`
- Cannot write directly to Windows filesystem from Linux sandbox
