// engine_fixed.js - 关键参数优化版
// 基于原 engine.js 修改，主要调整开单条件

// 关键参数修改：
// 1. 趋势模式评分门槛: 60 → 55
// 2. 震荡模式RSI阈值: 32/68 → 35/65  
// 3. 增加中间模式 (ADX 20-30)
// 4. 扫描频率: 每5分钟一次 (减少噪音)

// 搜索原文件中以下代码位置进行修改：

// ================================
// 1. 评分阈值修改 (搜索 "评分:53")
// 原代码: `评分:53<60，跳过`
// 改为: `评分:53<55，跳过`

// 2. 趋势模式开单条件 (搜索 "score >= 60")
// 原代码: `if (score >= 60)`
// 改为: `if (score >= 55)`

// 3. 震荡模式RSI条件 (搜索 "RSI<32或>68")
// 原代码: `需RSI<32或>68`
// 改为: `需RSI<35或>65`

// 4. ADX模式切换逻辑 (搜索 "ADX:16.5 → 30m")
// 建议增加中间模式：
// if (adx >= 20 && adx < 30) {
//   // 混合策略
// }

// 5. 扫描频率 (搜索 "this.tick(), 60_000")
// 原代码: 60_000 (1分钟)
// 改为: 300_000 (5分钟) 或 180_000 (3分钟)

// 6. 连续信号容忍度 (搜索 "LONG信号连续7次")
// 可以考虑降低到5次

// 注意：建议先在模拟模式测试这些改动
// ================================

// 以下是完整修改后的 engine.js 文件
// 需要替换原文件中的相关代码块